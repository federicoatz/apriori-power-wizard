# Built shinylive/webR static site (regenerated by CI on every push --
# see .github/workflows/deploy-shinylive.yml -- and too large to commit).
_site/

# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# macOS
.DS_Store

# Local Claude Code tooling state (not part of the app)
.claude/

# Manuscript drafts (paper.tex, APA submission version, bibliography, figures) --
# kept local only, not part of the public repo history.
manuscript/

# Reviewer feedback on the manuscript -- same reasoning as manuscript/ above.
Review.md
